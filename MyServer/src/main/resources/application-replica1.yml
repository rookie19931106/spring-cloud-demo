server:
  port: 8082
spring:
  application:
    name: eureka-server
eureka:
  instance:
    hostname: replica1
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://replica2:8083/eureka/
