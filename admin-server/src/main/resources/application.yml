spring:
  applcation:
    name: admin-server
server:
  port: 9301
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka/